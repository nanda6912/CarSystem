[phases.setup]
nixPkgs = ["jdk21", "maven"]

[phases.build]
cmds = ["mvn clean package -DskipTests -q"]

[start]
cmd = "java -jar target/*.jar"

[healthcheck]
path = "/actuator/health"
timeout = 100

[variables]
JAVA_OPTS = "-Xmx512m -Xms256m"
